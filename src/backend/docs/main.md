# Main Introductory Document

For all requests, except `login` and `logout`

Important header to add:
* Header `Authorization` and token to pass

If this header is not added: 

Response status is `401 Unauthorized`
